# Keybindings

## which-key
prefix + Space → help menu

## Panes
prefix + |   vertical split
prefix + -   horizontal split
prefix + h/j/k/l move between panes

## Sessions
prefix + Ctrl-s save
prefix + Ctrl-r restore
